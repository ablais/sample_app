<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <!--<%= gravatar_for @user %>-->
        <%= image_tag @user.avatar.url(:thumb)  %>
        <%= @user.name %>
        <br>
	<br>
        <br><%= @count_active %> active users in the system
        <br>You are user <%= @usernum %> out of <%= @count %> total users
      </h1>
    </section>
  </aside>
</div>
